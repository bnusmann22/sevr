import { useEffect, useState } from "react";
import { Download, ShieldCheck } from "lucide-react";

export default function ExternalCollaboratorPage() {
  const [seconds, setSeconds] = useState(3600);
  useEffect(() => { const timer = window.setInterval(() => setSeconds((current) => Math.max(0, current - 1)), 1000); return () => window.clearInterval(timer); }, []);
  if (seconds === 0) return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-6"><section className="w-full max-w-md border border-rose-200 bg-white p-8 text-center"><h1 className="text-lg font-bold text-slate-900">Link expired</h1><p className="mt-2 text-sm text-slate-600">This demo share link is no longer available.</p></section></main>;
  return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-6"><section className="w-full max-w-md space-y-5 border border-slate-200 bg-white p-8 shadow-sm"><div className="flex items-center gap-3"><ShieldCheck className="h-6 w-6 text-emerald-600" /><div><p className="text-xs font-semibold uppercase tracking-wider text-emerald-700">SeVR external access</p><h1 className="text-lg font-bold text-slate-900">Shared research file</h1></div></div><dl className="space-y-3 text-sm"><div className="flex justify-between gap-4"><dt className="text-slate-500">File</dt><dd className="font-semibold text-slate-900">draft_manuscript_v3.docx</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">Format</dt><dd className="text-slate-800">DOCX</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">TLP</dt><dd className="font-semibold text-amber-700">AMBER</dd></div><div className="flex justify-between gap-4"><dt className="text-slate-500">Expires in</dt><dd className="font-mono text-slate-800">{Math.floor(seconds / 60)}:{String(seconds % 60).padStart(2, "0")}</dd></div></dl><button type="button" className="flex w-full items-center justify-center gap-2 bg-slate-900 px-4 py-3 text-sm font-semibold text-white hover:bg-slate-700"><Download className="h-4 w-4" />Download file</button></section></main>;
}